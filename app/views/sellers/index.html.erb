<h1><%= current_user.first_name %>&nbsp;<%= current_user.last_name %></h1>
<%= link_to "Add a new Book", new_book_path, style: "color: purple;" %>
	<table class="table">
		<% @books.each do |book| %>
      <tr>
        <td><%= image_tag book.photo.url, :size => '60x60' %></td>
        <td><%= book.book_name %></td>
        <td><%= book.author_name %></td>
        <td><%= book.price %></td>
        <td><%= link_to 'Details', book %></td>
        <td><%= link_to 'Edit', edit_book_path(book) %></td>
        <td><%= link_to 'Remove', sellers_index_path, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr><br>
    <% end %>
  </table>